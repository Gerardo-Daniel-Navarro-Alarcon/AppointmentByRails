<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Productos con Inventario Bajo | AppointmentByRails</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="bg-light">

  <!-- Encabezado -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
      <a class="navbar-brand fw-bold" href="#">AppointmentByRails</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <%= link_to "Inicio", inicio_path, class: "nav-link text-white fw-semibold" %>
          </li>
          <li class="nav-item">
            <%= link_to "Empleados", employees_path, class: "nav-link text-white fw-semibold" %>
          </li>
          <li class="nav-item">
            <%= link_to "Servicios", services_path, class: "nav-link text-white fw-semibold" %>
          </li>
          <li class="nav-item">
            <%= link_to "Productos", products_path, class: "nav-link active fw-semibold" %>
          </li>
          <li class="nav-item">
            <%= link_to "Citas", appointments_path, class: "nav-link text-white fw-semibold" %>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Título de la página -->
  <section class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h1 class="text-primary fw-bold">Productos con Inventario Bajo</h1>
      <%= link_to "Volver a Productos", products_path, class: "btn btn-outline-secondary" %>
    </div>

    <!-- Lista de productos con inventario bajo -->
    <% if @low_stock_products.any? %>
      <div class="row row-cols-1 row-cols-md-2 g-4">
        <% @low_stock_products.each do |product| %>
          <div class="col">
            <div class="card h-100 shadow-sm">
              <div class="card-body">
                <h5 class="card-title"><%= product.name %></h5>
                <p class="card-text">Stock actual: <strong><%= product.stock %></strong></p>
                <p class="card-text">Umbral de inventario bajo: <%= product.low_stock_threshold || 5 %></p>
                <%= link_to "Ver Detalles", product_path(product), class: "btn btn-primary btn-sm" %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <!-- Mensaje cuando no hay productos con inventario bajo -->
      <div class="alert alert-success text-center mt-4">
        Todos los productos tienen inventario suficiente.
      </div>
    <% end %>
  </section>

  <!-- Pie de Página -->
  <footer class="bg-dark text-white text-center py-3 mt-5">
    <div class="container">
      <p>&copy; 2024 AppointmentByRails. Todos los derechos reservados.</p>
      <p>
        <a href="#" class="text-white text-decoration-none">Términos de Servicio</a> | 
        <a href="#" class="text-white text-decoration-none">Política de Privacidad</a>
      </p>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
